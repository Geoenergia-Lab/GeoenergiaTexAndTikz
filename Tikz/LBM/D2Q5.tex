\documentclass[tikz,border=5pt]{standalone}
\usepackage[portuguese,english]{babel}
%
% Useful packages
\usepackage{bm}
\usepackage[utf8]{inputenc}
\usepackage{pgfplots}
\usepackage{tikz}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta}
\begin{document}
\begin{tikzpicture}[scale=1, >=Stealth]
%
\begin{axis}[
    width = \textwidth,
    height = \textwidth,
    hide axis,
    clip = false,
    axis equal,
    domain = -1.5:1.5,
    xmin = -1.5,
    xmax = 1.5,
    ymin = -1.5,
    ymax = 1.5,
    samples = 1000,
    smooth,
]
% ---- 1. Draw the grid (edges of the 2×2×2 cube subdivided) ----
\tikzset{grid line/.style={gray, dashed}}
%
\draw[grid line] (-1,-1,0) -- (1,-1,0);
\draw[grid line] (-1,0,0) -- (1,0,0);
\draw[grid line] (-1,1,0) -- (1,1,0);
\draw[grid line] (-1,-1,0) -- (-1,1,0);
\draw[grid line] (0,-1,0) -- (0,1,0);
\draw[grid line] (1,-1,0) -- (1,1,0);
%
% ---- Coordinate system (placed at a corner for clarity) ----
%
\begin{scope}[thick, ->, >=Stealth]
  \draw (-1.2,-1.2,0) -- (-0.4,-1.2,0) node[above] {$x$};
  \draw (-1.2,-1.2,0) -- (-1.2,-0.4,0) node[above] {$y$};
\end{scope}
%
% ---- Draw population lines from centre to every other lattice point ----
%
\draw[->] (0,0,0) -- (1,0,0);
\draw[->] (0,0,0) -- (-1,0,0);
\draw[->] (0,0,0) -- (0,1,0);
\draw[->] (0,0,0) -- (0,-1,0);
%
\node[circle, fill=black, inner sep=1pt] at (0,0,0) {};
%
\end{axis}
\end{tikzpicture}
\end{document}