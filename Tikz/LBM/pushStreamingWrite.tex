\documentclass[tikz,border=5pt]{standalone}
\usepackage[portuguese,english]{babel}
%
% Useful packages
\usepackage{bm}
\usepackage[utf8]{inputenc}
\usepackage{pgfplots}
\usepackage{tikz}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta}
%
\begin{document}
\tikzset
{
  d3q27/line/.style={-{Stealth[length=4pt]}, thick, black!25},
  d3q27out/line/.style={-{Stealth[length=4pt]}, thick, black},
  bgarrow/.style={d3q27/line, lightgray, {Stealth[length=4pt]}-{Stealth[length=4pt]}, shorten >=1.5pt, shorten <=1.5pt},
  fgarrow/.style={d3q27out/line, shorten >=1.5pt},
  bgdoublearrow/.style={d3q27/line, lightgray, {Stealth[length=4pt]}-{Stealth[length=4pt]}, shorten >=1.5pt, shorten <=1.5pt},
  fgdoublearrow/.style={d3q27out/line, {Stealth[length=4pt]}-{Stealth[length=4pt]}, shorten >=1.5pt, shorten <=1.5pt}
}
\begin{tikzpicture}[scale=1]
    % Grid lines
    \foreach \i in {0,...,3}
    {
        \draw[lightgray!50, semithick] (-0.25, \i) -- (3.25, \i);
        \draw[lightgray!50, semithick] (\i, -0.25) -- (\i, 3.25);
    }
    
    % Background D2Q9 arrows
    \foreach \x in {0,...,2}
    {
        \foreach \y in {0,...,2}
        {
            \draw[bgarrow] (\x, \y+0.5) -- (\x+1, \y+0.5);
            \draw[bgarrow] (\x+0.5, \y) -- (\x+0.5, \y+1);
            \draw[bgarrow] (\x, \y) -- (\x+1, \y+1);
            \draw[bgarrow] (\x+1, \y) -- (\x, \y+1);
        }
    }
    
    % Foreground push arrows (write)
    \draw[fgarrow] (2.5, 1.5) -- (3, 1.5);
    \draw[fgarrow] (0.5, 1.5) -- (0, 1.5);
    \draw[fgarrow] (1.5, 0.5) -- (1.5, 0);
    \draw[fgarrow] (1.5, 2.5) -- (1.5, 3);
    \draw[fgarrow] (2.5, 2.5) -- (3, 3);
    \draw[fgarrow] (0.5, 2.5) -- (0, 3);
    \draw[fgarrow] (2.5, 0.5) -- (3, 0);
    \draw[fgarrow] (0.5, 0.5) -- (0, 0);
    
    % Nodes
    \foreach \x in {0,...,3}
    {
        \foreach \y in {0,...,3}
        {
            \fill[lightgray] (\x,\y) circle (1.5pt);
        }
    }
\end{tikzpicture}
\end{document}